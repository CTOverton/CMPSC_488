/*
const event = {
    title: 'Example',
    description: 'Sample',
};

const tags = [
    {key: 0, label: 'Admin'},
    {key: 1, label: 'VIP'}
];

firestore
    .collection('events')
    .add(event)
    .then(docRef => {
        // Do something with docRef
    })

tags.map((tag) => {
    firestore
        .collection('tags')
        .add(tag)
        .then(docRef => {
            // Do something with docRef
        })
});

// TODO: Add all Tag document references to event document*/
